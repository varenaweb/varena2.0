<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Varena CMS – Content list</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      background: #f4f5f7;
      font-family: Inter, system-ui, -apple-system, sans-serif;
      padding: 2rem;
    }

    .admin-card {
      background: white;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(15, 23, 42, 0.1);
      padding: 1.5rem;
      max-width: 720px;
      margin: 0 auto;
    }

    .admin-card h1 {
      margin-bottom: 1rem;
    }

    .entries {
      display: grid;
      gap: 0.75rem;
    }

    .entry {
      padding: 0.75rem 1rem;
      border-radius: 8px;
      border: 1px solid #e5e7eb;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .entry a {
      color: #2563eb;
      text-decoration: none;
      font-weight: 600;
    }

    .entry span {
      font-size: 0.9rem;
      color: #4b5563;
    }
  </style>
</head>
<body>
  <div class="admin-card">
    <h1>Varena CMS</h1>
    <p>Choose a page and language to edit the copy.</p>

    <div class="entries">
      <% entries.forEach((entry) => { %>
        <div class="entry">
          <span><%= entry.page %> / <%= entry.lang %></span>
          <a href="/admin/edit/<%= entry.page %>/<%= entry.lang %>">Edit</a>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>
