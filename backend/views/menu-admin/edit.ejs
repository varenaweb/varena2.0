<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Editor de menú - <%= slug %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/menu-admin.css">
</head>
<body class="menu-admin edit-page">
  <header class="menu-admin-topbar">
    <div>
      <p class="topbar-label">Menú de</p>
      <h1><%= slug %></h1>
    </div>
    <form method="post" action="/menu-qr/admin/logout">
      <button type="submit" class="btn btn-outline">Cerrar sesión</button>
    </form>
  </header>

  <main id="menu-app"
        data-slug="<%= slug %>"
        data-menu='<%- menuJson %>'
        data-cloud-name="<%= cloudinary?.cloudName || "" %>"
        data-upload-preset="<%= cloudinary?.uploadPreset || "" %>"
        data-can-upload="<%= cloudinary ? "1" : "0" %>">
    <noscript>
      <p class="alert alert-error">Este editor requiere JavaScript para funcionar.</p>
    </noscript>
    <div class="menu-admin-app">
      <section class="panel">
        <h2>Información general</h2>
        <div class="field-grid two">
          <label>
            Nombre del restaurante
            <input type="text" data-field="name" placeholder="Ej: Restaurante Demo">
          </label>
          <label>
            Moneda
            <input type="text" data-field="currency" placeholder="ARS, USD, etc.">
          </label>
        </div>
        <label>
          Texto para el precio sin impuestos
          <input type="text" data-field="taxNote" placeholder="Sin impuestos nacionales:">
        </label>
        <label>
          IVA (%)
          <input type="number" min="0" step="0.1" data-field="taxRate" placeholder="21">
        </label>
      </section>

      <section class="panel">
        <h2>Branding</h2>
        <div class="field-grid two">
          <label>
            Tagline
            <input type="text" data-branding="tagline">
          </label>
          <label>
            Colores (hex)
            <div class="field-grid three">
              <input type="text" data-branding="primaryColor" placeholder="#111827">
              <input type="text" data-branding="secondaryColor" placeholder="#2563EB">
              <input type="text" data-branding="accentColor" placeholder="#F97316">
            </div>
          </label>
        </div>
        <label>
          Descripción
          <textarea data-branding="description" rows="2"></textarea>
        </label>
        <div class="field-grid two">
          <div class="upload-field">
            <label for="logo-url">Logo URL</label>
            <div class="upload-control">
              <input id="logo-url" type="text" data-branding="logoUrl" placeholder="https://...">
              <div class="upload-actions">
                <button type="button" class="btn btn-light upload-trigger" data-upload-target="branding.logoUrl" data-upload-folder="branding">Subir</button>
                <a href="#" class="link-preview" target="_blank" data-preview-target="branding.logoUrl">Ver</a>
              </div>
            </div>
          </div>
          <div class="upload-field">
            <label for="hero-image">Imagen principal</label>
            <div class="upload-control">
              <input id="hero-image" type="text" data-branding="heroImage" placeholder="https://...">
              <div class="upload-actions">
                <button type="button" class="btn btn-light upload-trigger" data-upload-target="branding.heroImage" data-upload-folder="branding">Subir</button>
                <a href="#" class="link-preview" target="_blank" data-preview-target="branding.heroImage">Ver</a>
              </div>
            </div>
          </div>
        </div>
        <div class="field-grid two">
          <label>
            Dirección
            <input type="text" data-contact="address">
          </label>
          <label>
            Teléfono
            <input type="text" data-contact="phone">
          </label>
          <label>
            WhatsApp
            <input type="text" data-contact="whatsapp">
          </label>
          <label>
            Instagram
            <input type="text" data-contact="instagram" placeholder="@usuario">
          </label>
        </div>
        <% if (!cloudinary) { %>
          <p class="alert alert-error">Subida de imágenes deshabilitada. Configurá Cloudinary para habilitarla.</p>
        <% } %>
      </section>

      <section class="panel">
        <div class="panel-head">
          <div>
            <h2>Categorías e ítems</h2>
            <p>Agregá, renombrá o eliminá secciones e ítems libremente.</p>
          </div>
          <button type="button" class="btn" id="add-section">+ Nueva categoría</button>
        </div>
        <div id="sections-container" class="sections-container"></div>
      </section>

      <section class="panel">
        <div class="panel-head">
          <div>
            <h2>Notas</h2>
            <p>Mensajes que aparecen al final del menú.</p>
          </div>
          <button type="button" class="btn btn-light" id="add-note">+ Agregar nota</button>
        </div>
        <div id="notes-container" class="notes-container"></div>
      </section>

      <section class="panel actions">
        <div class="actions-buttons">
          <button type="button" class="btn btn-primary" id="save-menu">Guardar cambios</button>
          <button type="button" class="btn btn-outline" id="reload-menu">Descartar cambios</button>
        </div>
        <p id="save-status" class="save-status"></p>
      </section>
    </div>
  </main>

  <script src="https://widget.cloudinary.com/v2.0/global/all.js"></script>
  <script type="module" src="/menu-admin-app.js"></script>
</body>
</html>
